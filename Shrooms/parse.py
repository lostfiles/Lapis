from err import Errors

"""
class Parse:
    def __init__(self):
        self.tree = []
    
    def parse(self,toks):
        i = 0
        while i < len(toks):
            if toks[i]['id'] == 'name':
                if toks[i+1]['id'] == 'literal':
                    if toks[i+3]['id'] == 'literal':
                        if toks[i+2]['id'] == 'string':
                            print(toks[i+2]['value'])
            i+=1
"""